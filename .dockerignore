.git
.gitignore
.DS_Store
.ipynb_checkpoints
__pycache__
*.pyc

# Secrets — pass these at runtime instead (docker run -e / --env-file)
.env
.env.app
.env.mcp

logs
*.ipynb

# Front-end: build fresh inside the image; keep the build context small.
frontend/node_modules
frontend/dist
