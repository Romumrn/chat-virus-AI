# Dependencies for the FastAPI backend (backend/) — used by docker/Dockerfile.api.
# The MCP data server now lives in its own repo (viromeatlas_mcp); this app only
# talks to it over HTTP via fastmcp.Client, so it keeps fastmcp but no longer
# needs the MCP server's data stack (duckdb/pandas/numpy).
fastmcp>=3.4
plotly>=6.0
requests>=2.32
fastapi>=0.115
uvicorn[standard]>=0.30
PyJWT>=2.8
bcrypt>=4.0
python-multipart>=0.0.9
